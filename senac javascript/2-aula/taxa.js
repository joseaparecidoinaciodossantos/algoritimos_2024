/*Elaborar um programa que leia o valor de um jantar.Calcule e informe o valor da taxa do garçom(10%) e o
 valor total a ser pago.Vamos avnçar um pouco.Exemplo de possíveis dados de entrada e dos respectivos
  dados de saida exibidos pelo programa (e deixar para voçêenumera as etapas). 
  valor do jantar r$: 80.00
  taxa do garçom r$: 8.00
  total a pagar r$: 88.00
  (jantar * 10) /100; 

  let numero = prompt(80%)
  let number =   8.00
*/

  // le um dado da entrada

  let jantar = Number(prompt("digite o valor do jantar:"));
  let taxa = number ( prompt ("digite a taxa  do jantar 25");

  let garcom = (jantar * 10)/100; 
  let total= jantar + garcom;
  
  alert(`o valor do jantar e : ${jantar} valor total com a taxa do garcom e: ${total}` );
  alert(`o valor da taxa do garcom e : ${garcom}`);


  



  